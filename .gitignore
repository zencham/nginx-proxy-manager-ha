# Ansible retry files
*.retry

# Vault password file — never commit this
.vault_pass

# NPM proxy-config snapshots — live data, not source
backups/

# Local Ansible collection/role cache (installed via requirements.yml)
.ansible/
