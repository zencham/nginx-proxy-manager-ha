---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: npm-preflight-node1
    image: debian:trixie
    pre_build_image: true
    privileged: true
    groups:
      - ha_nodes
  - name: npm-preflight-node2
    image: debian:trixie
    pre_build_image: true
    privileged: true
    groups:
      - ha_nodes
provisioner:
  name: ansible
  inventory:
    group_vars:
      ha_nodes:
        node1_name: npm-preflight-node1
        node2_name: npm-preflight-node2
        node1_address: "{{ hostvars['npm-preflight-node1']['ansible_default_ipv4']['address'] }}"
        node2_address: "{{ hostvars['npm-preflight-node2']['ansible_default_ipv4']['address'] }}"
        pacemaker_cluster_name: npm_ha_test_cluster
        vip_address: 192.168.100.100
        vip_cidr: "24"
        drbd_backing_dev_app: /dev/nonexistent_app
        drbd_backing_dev_db: /dev/nonexistent_db
        drbd_resource_app: test_drbd_app
        drbd_resource_db: test_drbd_db
        container_timezone: UTC
        hacluster_password: test-hacluster-pass
        drbd_secret: test-drbd-secret
        mariadb_root_password: test-root-pass
        mariadb_npm_password: test-npm-pass
verifier:
  name: ansible
